// EDEN ENGINE Standard Library - ImGui Bindings
// This file is automatically included in generated C++ code

#ifndef EDEN_IMGUI_H
#define EDEN_IMGUI_H

#ifdef USE_IMGUI
#include "imgui.h"
#include "imgui_impl_glfw.h"
#include "imgui_impl_vulkan.h"
#endif

// ImGui functions are available through the ImGui namespace
// Common ImGui functions that can be called from EDEN:
// - ImGui::Begin(const char* name, bool* p_open = NULL, ImGuiWindowFlags flags = 0)
// - ImGui::End()
// - ImGui::Text(const char* fmt, ...)
// - ImGui::Button(const char* label, const ImVec2& size = ImVec2(0,0))
// - ImGui::NewFrame()
// - ImGui::Render()
// - ImGui::GetDrawData()

// These are handled as built-in functions in the compiler

#endif // EDEN_IMGUI_H

